# Hanveon — 이 저장소의 규칙

**Hanveon** — 설치도 가입도 없이 브라우저에서 바로 하는 작은 게임 모음.
Astro **정적** 사이트 → Cloudflare Pages (`main` 에 밀면 자동 배포) → **hanveon.com**.
저장소 `hoons-project/hanveon`. 도메인은 Porkbun에서 샀고 DNS만 Cloudflare가 맡는다.

주인은 코딩 초보다. **설명은 쉬운 말로 짧게 하고, 「어떻게」를 먼저 확인받고 손댄다.**

## 이름

- 한국말 **「한번」**(한번 해볼까)에서 왔다. 로고는 `Han` + `veon` 으로 색을 나눈다
- **브랜드 이름은 나라말마다 번역하지 않는다.** 어디서나 `Hanveon`. 설명글만 번역한다
- 게임 이름은 나라말마다 번역한다 (`src/data/games.ts`)

## 손대기 전에 읽을 것

**화면·색·글씨·짜임을 바꾸기 전에 `design/README.md` 를 먼저 읽고 거기에 맞춘다.**
그 폴더가 기준이다.

- `design/README.md` — 캐비닛 생김새, 색, 글씨 크기
- `design/new-game-checklist.md` — **새 게임 넣을 때 이 차례를 매번 따른다**

## 절대 안 바꾸는 것

- **로그인·회원가입·서버·데이터베이스 없음.** 최고 기록은 기기에만 남긴다
  (`window.storage` → 없으면 `localStorage`)
- **영어가 기본 언어.** HTML 에 처음 박히는 글은 영어다 (검색엔진이 자바스크립트 없이 읽는다)
- 나라말 여섯 — `en` `ko` `ja` `es` `fr` `zh`

## 주소 짜임

| 주소 | 무엇 |
|---|---|
| `/` | 영어 대문 |
| `/play/<slug>/` | 영어 게임 |
| `/ko/` `/ja/` `/es/` `/fr/` `/zh/` | 그 나라말 대문 |
| `/ko/play/<slug>/` 등 | 그 나라말 게임 |

**영어만 주소에 나라말이 안 붙는다.** 이미 인터넷에 나가 있는 주소라
`/en/` 으로 옮기면 그동안 쌓인 것이 끊긴다. 이건 `src/i18n/index.ts` 한 곳에서 정한다.

`trailingSlash: 'always'` 다. 주소 끝에 `/` 를 꼭 붙인다.

## 자료는 한 곳에

| 파일 | 무엇 |
|---|---|
| `src/i18n/index.ts` | 나라말 목록, 주소 만드는 함수 (`homePath` `gamePath` `altPaths`) |
| `src/i18n/ui.ts` | 사이트 전체에 쓰는 글 (발문, 「하기」, 「게임 목록」…) |
| `src/data/games.ts` | 게임 목록 — 이름·설명·색·목록 그림 |
| `src/games/<slug>/strings.ts` | **그 게임 안에서만** 쓰는 글 |

**같은 것을 두 곳에 적지 않는다.** 게임 이름은 `games.ts` 에만 있고,
게임 안의 시작 화면 글은 그 게임 `strings.ts` 에만 있다.

게임 이름은 두 조각으로 저장한다 — `name: { en: ['Hoppy', 'Jump'], … }`.
뒤 조각에 색이 들어가기 때문이다. 이어 붙일 때는 `fullName(game, lang)` 을 쓴다
(일본말·중국말은 사이를 안 띄운다).

## 게임은 이렇게 생겼다

```
src/games/<slug>/
  Game.astro    HUD·캔버스·덮개 화면·손가락 단추 + 그 게임만 쓰는 CSS + 게임 자바스크립트
  strings.ts    그 게임 안의 글, 나라말 여섯 개
```

`src/pages/play/[slug].astro` (영어) 와 `src/pages/[lang]/play/[slug].astro` 가
`src/games/registry.ts` 를 보고 주소를 만든다. **폴더를 만들면 저절로 붙는다.**

### 「게임 하나는 파일 하나」는 이제 아니다 — 왜

단계 1까지는 게임이 `public/play/<slug>/index.html` 통짜 파일이었고,
그 파일 하나만 열어도 돌아갔다. 단계 3에서 **일부러 뒤집었다.**

- 나라말 여섯 개가 **주소마다 따로** 나와야 구글이 여섯 페이지로 본다.
  전에는 한 페이지 안에서 자바스크립트로 글을 갈아 끼웠다
- 그래서 게임 안의 `LANGS` `STR` `pickLang()` `apply()` 가 다 없어졌다.
  글은 처음부터 박혀서 나온다. 게임 코드가 그만큼 짧아졌다
- 캐비닛 CSS 가 네 파일에 복사돼 있던 것도 `src/styles/game.css` 한 곳으로 합쳤다

대신 게임 파일 하나만 따로 열어서는 안 돌아간다. **그건 받아들인 값이다.**

## 커밋 전에

```bash
npm run build
```

빌드가 통과해야 민다. `main` 에 밀면 Cloudflare Pages 가 알아서 배포한다.
혼자 하는 프로젝트라 `main` 에 바로 커밋해도 된다.
`package-lock.json` 은 저장소에 넣고, `node_modules/` `dist/` `_notes/` 는 넣지 않는다.

## 넘어가는지 확인할 때

**주소가 제대로 넘어가는지는 브라우저로 직접 주소창을 봐야 한다.**
프로그램으로 페이지를 받아오는 도구는 301 을 조용히 따라가서, 안 되는데도 된 것처럼 보인다.
(www → hanveon.com 을 두 번 틀리게 말한 적이 있다.)

## 검은 고양이

회사 공통 마스코트다 (DAYREACH·Korea Soundscape와 같은 고양이).
**사이트 브랜드 자리에만 쓴다** — 대문 한 귀퉁이, 빈 화면, 404 페이지.
**게임 안에는 넣지 않는다.** 게임마다 자기 캐릭터가 있고, 작은 크기에서는 고양이가 식별되지 않는다.
기준은 노션 「검은 고양이 공통 브랜드 마스코트」 문서.

## 다음 단계

전체 계획은 노션 「Hanveon — 웹 게임 사이트」와 맥북 `게임사이트-총단계.md` 에 있다.
지금은 **전체 9단계 중 1·2·3단계**를 마친 상태다. 다음은 단계 4.
