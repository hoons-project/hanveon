# 대문 대표 그림이 잘리는 것 고치기

> **급하다.** 카톡·트위터에 `hanveon.com` 을 붙이면 나오는 그림이 지금 깨져 있다.
> 게임 여덟 개 때부터(휙휙 패스) 그랬고, 아홉 개인 지금은 하나가 아예 안 나온다.

## 무엇이 문제인가

`scripts/make-og.mjs` 의 게임 그림 줄이 **132px 고정 · 줄바꿈 없음**이다.

```
게임 7개 → 7×132 + 6×18 = 1104px   들어감
게임 8개 →                1254px   여덟째가 반쯤 잘림
게임 9개 →                1332px   아홉째는 화면 밖
```

그림은 1200px 이다. **아홉째가 화면 밖이라 다시 만들어도 그림이 안 바뀐다** —
`home` 그림 여섯 장이 바이트까지 똑같이 나온다.

**이건 지시서를 쓴 쪽의 잘못이다.** 단계 4에서 게임 여섯 개에 맞춰 짜 놓고
게임이 늘 때를 생각 안 했다. 클로드 코드는 「손댈 파일」에 없어서 안 건드린 것이 맞다.

## 어떻게 고치나

**게임 개수를 보고 크기와 줄 수를 정한다.** 일곱까지는 한 줄, 그 위는 두 줄.

| 게임 수 | 한 줄에 | 크기 |
|---|---|---|
| 6 | 6개 | 132px |
| 7 | 7개 | 129px |
| **9 (지금)** | **5개 · 두 줄** | **96px** |
| 14 | 7개 · 두 줄 | 96px |
| 20 | 10개 · 두 줄 | 85px |
| 22 | 11개 · 두 줄 | 76px |

22개까지 안 잘린다. 그 위로 가면 **뒤를 빼고 화면에 알린다** — 조용히 빠지지 않는다.

> **이 지시서는 실제로 넣고 돌려서 확인했다.** 아홉 개가 두 줄(5+4)로 다 들어가고,
> 세로도 안 넘치고, 게임 그림 54장은 전과 같다. 아래 코드가 그때 쓴 것 그대로다.

---

## 손댈 파일

1. `scripts/make-og.mjs` — 세 군데
2. `design/new-game-checklist.md` — 확인 한 줄
3. 그러고 나서 `node scripts/make-og.mjs`

**다른 것은 아무것도 안 건드린다.** 게임·대문·읽는 쪽·글 전부 그대로.

---

## 1. `scripts/make-og.mjs`

### 1-1. CSS 의 대문 칸을 갈아 끼운다

`CSS` 안의 이 덩어리를

```css
  /* 대문 그림 — 그림 여섯을 한 줄로 늘어놓는다 */
  body.home{flex-direction:column;align-items:flex-start;justify-content:center;gap:22px}
  .home .name{font-size:104px}
  .home .desc{font-size:29px;max-width:1000px}
  .arts{display:flex;gap:18px;margin-top:10px}
  .arts .art{width:132px;height:132px;border-radius:16px;border:1px solid ${BEBE}}
```

아래로 바꾼다. **크기를 CSS 에서 빼고 자바스크립트가 정하게 한다.**

```css
  /* 대문 그림 — 게임 그림을 늘어놓는다.
     크기와 줄 수는 artsLayout() 이 게임 개수를 보고 정한다.
     예전에는 132px 한 줄 고정이라 여덟 개부터 잘려 나갔다. */
  body.home{flex-direction:column;align-items:flex-start;justify-content:center;gap:20px}
  .home .name{font-size:96px}
  .home .desc{font-size:28px;max-width:1000px}
  .arts{display:flex;flex-wrap:wrap;margin-top:6px}
  .arts .art{flex:none;border-radius:14px;border:1px solid ${BEBE}}
```

> **주의 — 이 덩어리는 백틱 템플릿 문자열 안이다.**
> 주석에 백틱(`` ` ``)을 쓰면 문자열이 거기서 끊겨 빌드가 깨진다.
> 위 주석에 `artsLayout()` 을 백틱 없이 적어 둔 것이 그 때문이다. **그대로 쓴다.**
> (지시서를 쓰면서 실제로 이 오류를 냈다.)

세로를 줄인 세 곳 — `gap` 22→20, 이름 104→96, 설명 29→28 —
**두 줄이 세로로 안 넘치게 하려고 그런 것이다.** 되돌리지 않는다.

### 1-2. `gameCard` 와 `homeCard` 사이에 넣는다

```js
/* ── 대문 그림의 게임 그림 늘어놓기 ────────────────────
   게임이 늘어도 안 잘리게, 개수를 보고 크기와 줄 수를 정한다.
   일곱까지는 한 줄, 그 위는 두 줄. 두 줄이 넘치면 뒤를 빼고 알린다. */
const ARTS_W = 1000;        // 설명 글 칸과 같은 너비
const ARTS_GAP = 16;
const ARTS_MAX1 = 132;      // 한 줄일 때 최대 크기
const ARTS_MAX2 = 96;       // 두 줄일 때 최대 크기 (세로가 모자란다)
const ARTS_MIN = 70;        // 이보다 작아지면 뒤를 잘라낸다

function artsLayout(n) {
  const rows = n <= 7 ? 1 : 2;
  const max = rows === 1 ? ARTS_MAX1 : ARTS_MAX2;
  let shown = n;
  for (;;) {
    const per = Math.ceil(shown / rows);
    const size = Math.min(max, Math.floor((ARTS_W - (per - 1) * ARTS_GAP) / per));
    if (size >= ARTS_MIN || shown <= 1) {
      return { size, per, shown, rowW: per * size + (per - 1) * ARTS_GAP };
    }
    shown--;
  }
}

/* 자리가 모자라 뺀 것이 있으면 조용히 넘어가지 않고 알린다 */
{
  const L = artsLayout(GAMES.length);
  if (L.shown < GAMES.length) {
    console.warn(`[og] 대문 그림 — 게임 ${GAMES.length}개 중 ${L.shown}개만 넣었다. ` +
                 `뒤 ${GAMES.length - L.shown}개가 빠진다. 줄 수를 늘리거나 크기를 줄여야 한다.`);
  }
}
```

### 1-3. `homeCard` 를 갈아 끼운다

```js
function homeCard(lang) {
  const t = UI[lang];
  const L = artsLayout(GAMES.length);
  return `<!doctype html><html lang="${lang}"><head><meta charset="utf-8">
<style>${CSS(lang)}</style></head><body class="home">
<div class="name">Han<span style="color:${RAUSCH}">veon</span></div>
<div class="desc">${esc(t.metaDesc)}</div>
<div class="arts" style="width:${L.rowW}px;gap:${ARTS_GAP}px">${GAMES.slice(0, L.shown)
  .map((g) => `<div class="art" style="width:${L.size}px;height:${L.size}px">${g.art}</div>`).join('')}</div>
<div class="tag">${esc(t.freeTag)}</div>
</body></html>`;
}
```

칸 너비를 `rowW` 로 못 박는 것이 요점이다. 그래야 한 줄에 정확히 `per` 개가 들어간다.

---

## 2. `design/new-game-checklist.md`

6번 「대표 그림 다시 만들기」의 이 줄을

```
**안 돌리면 새 게임만 그림이 없다.** 만들고 나서 `public/og/` 에서 한 장은 눈으로 연다
```

아래로 바꾼다. **게임 그림만 보면 이번 같은 것을 못 잡는다.**

```
**안 돌리면 새 게임만 그림이 없다.** 만들고 나서 **두 장**을 눈으로 연다 —
새 게임 그림 한 장, 그리고 **`home-ko.png`**. 대문 그림에는 게임 그림이 다 들어가는데,
자리가 모자라면 뒤가 잘린다. 2026-08-24에 게임 여덟 개부터 잘리고 있던 것을
두 판이나 지나서야 알았다. 새 게임을 넣을 때마다 대문 그림을 꼭 본다
```

---

## 3. 그림 다시 만들기

```bash
node scripts/make-og.mjs
```

---

## 하지 말 것

- 게임 그림(`gameCard`)의 생김새를 건드리지 않는다. 54장은 전과 같아야 한다
- 위 주석에 백틱을 넣지 않는다 (1-1의 주의)
- 세로를 줄인 세 값을 되돌리지 않는다
- 게임·대문·읽는 쪽·글을 건드리지 않는다
- 자리가 모자랄 때 **조용히 빼지 않는다.** 알리는 줄을 지우지 않는다

---

## 끝났다는 신호

- `public/og/home-ko.png` 를 열면 **게임 그림 아홉 개가 다 보인다.** 두 줄이다
- 오른쪽·아래로 잘린 것이 없다
- 게임 그림 54장은 전과 같다

---

## 확인 차례

```bash
node scripts/make-og.mjs
npm run build
```

**대문 그림 여섯 장을 다 눈으로 연다.** 이게 이번 확인의 전부다.

```bash
ls -la public/og/home-*.png
```

여섯 장이 **서로 다른 크기**여야 한다. 나라말마다 설명 글이 다르기 때문이다.
바이트까지 같은 것이 있으면 아직 뭔가 잘린 것이다.

**게임 개수가 늘어도 되는지 셈으로 본다.**

```bash
node -e '
const W=1000,G=16,A1=132,A2=96,MIN=70;
function L(n){const r=n<=7?1:2,m=r===1?A1:A2;let s=n;
  for(;;){const p=Math.ceil(s/r),z=Math.min(m,Math.floor((W-(p-1)*G)/p));
    if(z>=MIN||s<=1)return{z,p,s};s--;}}
for(const n of [7,8,9,10,14,20,22,24]){const x=L(n);
  console.log(`게임 ${n}개 → 한 줄 ${x.p}개 · ${x.z}px · 보임 ${x.s}`);}
'
```

`22개`까지는 「보임」이 게임 수와 같아야 하고, `24개`에서만 줄어야 한다.

- [ ] `home-ko.png` 에 게임 아홉 개가 다 보인다
- [ ] 여섯 나라말 대문 그림이 서로 크기가 다르다
- [ ] 한국말·일본말·중국말이 네모(두부)로 안 나온다
- [ ] 게임 그림 한 장을 열어 전과 같은지 본다
- [ ] 위 셈에서 22개까지 안 빠진다
- [ ] `npm run build` 통과

다 되면 커밋하고 민다.

```
git add -A
git commit -m "대문 대표 그림 — 게임이 늘어도 안 잘리게"
git push
```
